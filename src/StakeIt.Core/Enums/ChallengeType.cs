namespace StakeIt.Core.Enums;

public enum ChallengeType
{
    Duel,              // 1v1 race mode (qui fait le plus)
    DuelAsymmetric,    // Bet mode (parier contre quelqu'un)
    TeamBattle,        // Équipes qui s'affrontent
    Ladder             // Classement/league ouvert
}
